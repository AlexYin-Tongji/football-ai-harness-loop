# AI 开发协作约束

- 修改前先阅读 `README.md`、相关领域文档和 ADR。
- 不把 LLM 对话历史当作业务数据库或事实来源。
- 不添加未经 Source Registry 批准的生产抓取源。
- 不提交密钥、原始付费数据、完整新闻正文、模型二进制或用户数据。
- 生成文本必须保留结构化引用；概率必须来自预测服务，不能由 LLM 重写。
- 所有循环必须有最大步骤、最大重试、超时、成本预算和人工回退。
- 发布、文章更正、比赛结果写入和模型晋级必须经过显式权限检查。
- 变更领域边界、工作流控制或模型治理时新增/更新 ADR。
- 优先小而可验证的改动；提交前运行相关测试并报告未验证项。

