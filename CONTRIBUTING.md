# 研发协作规范

## 分支与提交

- `main` 始终保持可发布，禁止直接提交功能开发。
- 分支：`feat/<topic>`、`fix/<topic>`、`docs/<topic>`、`chore/<topic>`。
- 提交采用 Conventional Commits，例如 `feat(prediction): add calibrated poisson baseline`。
- 一个 PR 只解决一个明确问题；涉及架构决策时同时新增或更新 ADR。

## 开发流程

1. Issue 写清用户价值、范围、验收和风险。
2. 从最新 `main` 创建分支。
3. 实现代码、测试、数据迁移与文档。
4. 本地运行格式、静态检查、单元/集成测试。
5. 打开 Draft PR，填写影响、验证、数据/模型/合规变化。
6. 至少一名代码所有者批准；高风险内容/模型变更需要领域负责人批准。
7. CI 通过后 Squash merge；上线异常通过回滚提交处理，不重写共享历史。

## 完成定义

- 验收标准全部满足。
- 测试覆盖正常路径、失败路径和降级路径。
- 新的数据源已登记许可、配额、负责人和停用开关。
- 新的 LLM 步骤含 schema、超时、预算、评估与人工回退。
- 新的模型版本含时间外回测、校准、模型卡和回滚方案。
- 日志不含密钥、完整受版权保护正文或不必要的个人数据。
- 用户可见变化和运营方式已经文档化。

## 评审重点

1. 事实是否能追溯到原始来源。
2. 是否存在训练/回测时间泄漏。
3. 模型或智能体是否能绕过发布权限。
4. 重试是否幂等且有上限。
5. 外部服务失败时是否安全降级。

当前文档阶段可运行 `python scripts/check_docs.py` 检查本地链接、尾随空格和文件结尾；PR 会执行同样的检查。
